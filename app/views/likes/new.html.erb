<!-- Validation failure messages -->
<% if @like.errors.any? %>
  <% @like.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Like</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_like" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for smack_id -->
      <div class="form-group">
        <label for="smack_id" class="control-label">
          Smack
        </label>

        <input type="text" id="smack_id" name="smack_id" placeholder="Smack" class="form-control" value="<%= @like.smack_id %>">
      </div>

      <!-- Label and input for user_id -->
      <div class="form-group">
        <label for="user_id" class="control-label">
          User
        </label>

        <input type="text" id="user_id" name="user_id" placeholder="User" class="form-control" value="<%= @like.user_id %>">
      </div>

      <!-- Label and input for response_id -->
      <div class="form-group">
        <label for="response_id" class="control-label">
          Response
        </label>

        <input type="text" id="response_id" name="response_id" placeholder="Response" class="form-control" value="<%= @like.response_id %>">
      </div>

      <button class="btn btn-success">
        Create Like
      </button>
      or
      <a href="/likes">Cancel</a>
    </form>
  </div>
</div>
